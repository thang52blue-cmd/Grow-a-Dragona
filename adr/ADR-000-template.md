# ADR-000: Template

Copy this file to `ADR-<NNN>-<kebab-slug>.md` for a new decision. Add one line to
`memory-bank/decisionLog.md` pointing at it — that log is append-only; never rewrite a past entry.

## Status

Proposed | Accepted | Superseded by ADR-NNN

## Context

What problem forced this decision? What constraints (technical, scope, GDD, prior ADRs) applied?

## Decision

What was decided, stated as a concrete, checkable rule — not a vague direction.

## Consequences

What this makes easier, what it makes harder, and what it explicitly rules out. Note any follow-up
work the decision creates (e.g. a backlog item).
