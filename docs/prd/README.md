# PRDs

One file per feature: `<slug>.md`, produced by `workflow: to-prd` after `workflow: grill` has
reached alignment. Each PRD must have testable acceptance criteria — see
`.agent/workflows/to-prd.md` for the required shape. No PRDs exist yet.
